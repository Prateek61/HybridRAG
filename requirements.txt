# --- Web API ---
fastapi==0.136.3
uvicorn==0.49.0
python-multipart==0.0.31   # required by FastAPI for file uploads (UploadFile/File)

# --- Vector store ---
chromadb==1.5.9

# --- BM25 ---
rank_bm25==0.2.2

# --- Embeddings (local, ONNX, no torch) ---
fastembed==0.7.4           # reuses onnxruntime already bundled by chromadb; default model: BAAI/bge-small-en-v1.5

# --- LLM (free Groq API) ---
groq==1.4.0

# --- Document loaders ---
pypdf==6.12.2              # PDF
python-docx==1.2.0         # .docx / .doc
beautifulsoup4==4.14.3     # HTML
pytesseract==0.3.13        # image OCR (needs system `tesseract` binary)
pillow==11            # image handling for OCR


# --- Config ---
python-dotenv==1.2.2

#--- Table formatting ---
tabulate==0.9.0
